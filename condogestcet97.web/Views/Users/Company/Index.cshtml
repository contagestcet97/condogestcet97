@model IEnumerable<condogestcet97.web.Data.Entities.Users.Company>

@{
    ViewData["Title"] = "Companies";
}

<h1>Companies</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor((condogestcet97.web.Data.Entities.Users.Company c) => c.Name)
            </th>
            <th>
                @Html.DisplayNameFor((condogestcet97.web.Data.Entities.Users.Company c) => c.Address)
            </th>
            <th>
                @Html.DisplayNameFor((condogestcet97.web.Data.Entities.Users.Company c) => c.Phone)
            </th>
            <th>
                @Html.DisplayNameFor((condogestcet97.web.Data.Entities.Users.Company c) => c.FiscalNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Address</td>
                <td>@item.Phone</td>
                <td>@item.FiscalNumber</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-action="Create" class="btn btn-primary">Create New</a>
